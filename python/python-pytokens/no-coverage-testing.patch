--- a/setup.cfg
+++ b/setup.cfg
@@ -36,7 +36,6 @@ dev =
     build
     mypy
     pytest
-    pytest-cov
     setuptools
     tox
     twine
@@ -45,6 +44,3 @@ dev =
 [options.package_data]
 pytokens =
     py.typed
-
-[tool:pytest]
-addopts = --cov --cov-report=term-missing
