diff --unified --recursive --text --new-file xarray-2026.01.0.orig/xarray/core/dtypes.py xarray-2026.01.0/xarray/core/dtypes.py
--- xarray-2026.01.0.orig/xarray/core/dtypes.py	2026-01-28 18:47:15.000000000 +0100
+++ xarray-2026.01.0/xarray/core/dtypes.py	2026-02-01 01:38:30.597749956 +0100
@@ -278,10 +278,11 @@
             )
             if isinstance(result_type, np.dtype):
                 np_result_types.add(result_type)
-        except TypeError:
+        except (TypeError, ValueError):
             # passing individual objects to xp.result_type (i.e., what `array_api_compat.result_type` calls) means NEP-18 implementations won't have
             # a chance to intercept special values (such as NA) that numpy core cannot handle.
             # Thus they are considered as types that don't need promotion i.e., the `arr_or_dtype` that rose the `TypeError` will not contribute to `np_result_types`.
+            # Note: NumPy 2.x raises ValueError for dtypes it can't convert (e.g., PeriodDtype).
             pass
 
     if np_result_types:
diff --unified --recursive --text --new-file xarray-2026.01.0.orig/xarray/core/utils.py xarray-2026.01.0/xarray/core/utils.py
--- xarray-2026.01.0.orig/xarray/core/utils.py	2026-01-28 18:47:15.000000000 +0100
+++ xarray-2026.01.0/xarray/core/utils.py	2026-02-01 01:42:47.310951879 +0100
@@ -213,7 +213,8 @@
 
     try:
         result_type = dtypes.result_type(*original_coords)
-    except TypeError:
+    except (TypeError, ValueError):
+        # ValueError raised by NumPy 2.x for dtypes it can't convert (e.g., PeriodDtype)
         pass
     else:
         if result_type.kind in "SU":
