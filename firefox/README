* so-bump of dependencies

  If you don't have a choice and need to run outside a chroot or container,
  remember to uninstall Firefox prior to its build. It'll otherwise sometimes
  pull in old SO links from its own installation. (libffi)
  
