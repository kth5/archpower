# Upstream does not use setup.py to build and install Python packages,
# therefore it is too difficult to prepare a package for Python3 reusing
# the same source.
openbabel source: python-foo-but-no-python3-foo python-openbabel
