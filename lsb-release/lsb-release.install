post_install() {
  echo "Arch Linux POWER release" >> /etc/arch-release
}

post_upgrade() {
  sed -e "/Arch Linux POWER release/d" -i /etc/arch-release
  echo "Arch Linux POWER release" >> /etc/arch-release
}

pre_remove() {
  sed -e "/Arch Linux POWER release/d" -i /etc/arch-release
}
