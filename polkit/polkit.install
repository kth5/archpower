pre_upgrade() {
  if (( $(vercmp "$2" 124-2) < 0 )); then
    install -d -o root -g root -m 755 usr/share/polkit-1/rules.d
  fi

  if (( $(vercmp "$2" 127-1) >= 0 && $(vercmp "$2" 127-3) < 0 )); then
    systemctl disable polkit-agent-helper.socket
  fi
}

# vim:set ft=bash sw=2 sts=-1 et:
